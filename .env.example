# Copy this file to .env and fill in your values
# Required for core functionality
HELIUS_API_KEY=
BITQUERY_API_KEY=
NEXT_PUBLIC_SOLANA_CLUSTER=mainnet-beta

# Optional / Feature-gated
# Used for Jupiter swaps/referrals (if enabled)
JUPITER_REFERRAL_ACCOUNT=

# Used if Moralis endpoints are called
MORALIS_API_KEY=

# EVM examples / fallbacks
ETH_RPC_URL=

# AI providers (only if you wire routes to them)
OPENAI_API_KEY=

# Wallet adapters / providers
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Site URL (used for links, redirects)
NEXT_PUBLIC_SITE_URL=https://keystone.stauniverse.tech

# Notes:
# - Atlas can run with degraded functionality without some provider keys.
# - Transactions are always signed client-side; no private keys are stored server-side.